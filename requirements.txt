# Collector prototype dependencies.
# Pinned per project convention: a stable version at least ~1 week old /
# one minor release back from bleeding edge, not the latest release.

# WS client (docs/04-architecture/01-stack.md: `websockets`, not `aiohttp`).
websockets==13.1

# Redis Streams broker client with asyncio support (`redis.asyncio`).
redis==5.0.8

# REST snapshot fetch (GET /api/v3/depth) -- async HTTP client.
httpx==0.27.2

# Normalizer prototype dependencies.
# ClickHouse client (docs/04-architecture/01-stack.md: `clickhouse-connect`,
# native asyncio since 0.12.0). Pinned one minor release back from the
# current latest (1.8.0) per project convention. `[async]` extra pulls in
# aiohttp, required by clickhouse_connect.driver.asyncclient.AsyncClient.
clickhouse-connect[async]==1.7.2
aiohttp==3.10.10

# API prototype dependencies (REST + MCP serving layer,
# docs/04-architecture/00-overview.md §6.3).
# Versions checked against PyPI at implementation time (2026-09-13); pinned one
# minor release back from the then-current latest per project convention:
#   fastapi latest 0.141.1 (2026-07-29) -> pinned 0.140.13 (last 0.140.x patch)
#   uvicorn latest 0.52.4  (2026-08-19) -> pinned 0.51.0  (last 0.51.x release)
# `mcp` (official Anthropic MCP Python SDK) is pinned to the 1.x line, not the
# newer 2.x line (latest 2.2.0) -- mcp 2.0 renamed `FastMCP` to `MCPServer` and
# changed the mounting API (confirmed by importing 2.1.1 in a scratch venv:
# `mcp.server.fastmcp` raises ModuleNotFoundError with a migration-guide
# pointer), which this codebase's design (mcp.server.fastmcp.FastMCP,
# .streamable_http_app()) relies on. 1.30.0 (2026-09-07) was <1 week old at
# pin time, so pinned one release back: 1.29.1 (2026-08-24).
fastapi==0.140.13
uvicorn==0.51.0
mcp==1.29.1
