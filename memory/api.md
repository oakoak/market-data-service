# Memory — api

Rules: see memory/README.md. One entry per fact, 1-4 lines, always with a source,
never duplicating something already stated in docs/.

- [2026-09-14] Phase B: implemented `get_derivatives_metrics` (was the deliberately-deferred 5th MVP tool, docs/03-mvp-scope.md). `src/api/queries.py` adds `get_derivatives_metrics(client, *, exchange, segment, symbol, ts_from, ts_to)` -- queries `market_data.mark_price`, `market_data.open_interest`, `market_data.liquidations` independently (no SQL join, per normalizer-agent's handoff note in this file -- three genuinely independent cadences) and combines client-side into `{exchange, segment, symbol, mark_price: [...], open_interest: [...], liquidations: [...]}`. `src/api/mcp_tools.py` registers it as `@mcp.tool() get_derivatives_metrics(symbol, ts_from=None, ts_to=None, exchange=None, segment=None)`, same param convention as `get_trades`. `src/api/rest.py` adds `GET /derivatives?symbol=&ts_from=&ts_to=&exchange=&segment=` with `DerivativesMetricsResponse` (nested `MarkPriceOut`/`OpenInterestOut`/`LiquidationOut` Pydantic models) -- both protocols call the same `queries.get_derivatives_metrics`, no logic drift. `segment` for USDT-M perp is `"usdtm"` (verified no stale `"usdtm_perp"` anywhere in src/api/). (source: this session, src/api/{queries,mcp_tools,rest}.py)
- [2026-09-14] `src/api/known_limitations.py` populated with the `liquidation_partial_coverage` entry (was an empty list -- prototype now collects Binance `forceOrder` liquidations, migration 005): `{exchange: "binance", type: "liquidation_partial_coverage", affected_channel: "liquidations", source_channel: "forceOrder", symbol: null, since_ts: "2026-09-14T00:00:00.000Z", description: "..."}`, structure copied verbatim from docs/04-architecture/00-overview.md §6.3.1's example. Served at existing `GET /known-limitations` (route already existed, unchanged). Docs updated in the same change: docs/07-local-dev.md's REST-endpoint examples and MCP-endpoint tool-count text (was "4 tools... get_derivatives_metrics intentionally not implemented", now "all 5 tools"). docs/03-mvp-scope.md needed no change -- it already listed the tool without a "deferred" caveat. (source: this session, src/api/known_limitations.py, docs/07-local-dev.md)
